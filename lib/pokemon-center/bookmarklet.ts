/**
 * Builds a self-contained PokeWatch bookmarklet.
 * Pokemon Center CSP blocks loading /pc-queue-watch.js and fetch() to CollecTools,
 * so the monitor is inlined and reports via window.open() / <a target=_blank> beacons.
 * Sync is confirmed via postMessage from the report page (not merely opening a pop-up).
 */
export function buildQueueWatchBookmarklet(input: {
  origin: string
  sessionId: string
  token: string
}): string {
  const origin = input.origin.replace(/\/$/, "")
  const sid = JSON.stringify(input.sessionId)
  const tok = JSON.stringify(input.token)
  const base = JSON.stringify(origin)

  const body = buildMonitorScript({ base, sid, tok })
  return `javascript:${body}`
}

/** Compact in-page monitor — keep one line for bookmark URL length limits. */
function buildMonitorScript(vars: { base: string; sid: string; tok: string }): string {
  const { base, sid, tok } = vars
  return `(()=>{if(window.__pcQueueWatchActive){alert("PokeWatch is already running on this tab.");return}window.__pcQueueWatchActive=true;var O=${base},SID=${sid},TOK=${tok},lastLive=false,lastChallenge=false,lastRep=null,lastAt=0,sticky=[],HB=12000,SCAN=4000,syncOk=false,confirmT=null,pendingState=null;var b=document.createElement("div");b.textContent="PokeWatch — syncing…";b.style.cssText="position:fixed;bottom:12px;right:12px;z-index:2147483647;padding:8px 12px;border-radius:999px;background:#111827;color:#f9fafb;font:600 12px/1.2 system-ui,sans-serif;box-shadow:0 8px 24px rgba(0,0,0,.35);cursor:pointer;max-width:260px";document.documentElement.appendChild(b);function log(){try{console.log.apply(console,["[CollecTools PokeWatch]"].concat([].slice.call(arguments)))}catch(e){}}function push(a,id,l,c){if(a.some(function(s){return s.id===id}))return;a.push({id:id,label:l,confidence:c})}function merge(dom){var m=[];sticky.forEach(function(s){push(m,s.id,s.label,s.confidence)});dom.forEach(function(s){push(m,s.id,s.label,s.confidence)});return m}function isChallengePage(t){return/are you human|verify you are human|confirm you are human|please verify you(?:'|')?re a human/i.test(t)||/g-recaptcha|recaptcha-anchor|hcaptcha|h-captcha|cf-turnstile/i.test(t)||/geo\\.captcha-delivery\\.com|distil_captcha|incapsula.*challenge/i.test(t)||/select all (?:images|squares|tiles)|pick (?:all )?(?:images|squares)|tap the matching/i.test(t)||/_Incapsula_Resource[\\s\\S]{0,600}(?:captcha|human|verify)/i.test(t)}function scan(){var dom=[],t=document.documentElement.innerHTML,h=location.href;if(/queue-it\\.net/i.test(h)||/waitingroom|waiting-room/i.test(h))push(dom,"waiting-room-url","Waiting room URL",100);if(/queue-it\\.net|queue-it\\.js|queueit/i.test(t))push(dom,"queue-it","Queue-it assets",100);if(/virtual queue|waiting room|hi,?\\s*trainer/i.test(t))push(dom,"queue-copy","Queue page copy",80);if(/"pos"\\s*:\\s*\\d+/.test(t)&&/"pending"\\s*:\\s*1/.test(t))push(dom,"incapsula-queue","Incapsula queue payload",90);if(isChallengePage(t)){push(dom,"imperva-human-verify","Imperva human verification",95);if(/g-recaptcha|recaptcha-anchor|hcaptcha|h-captcha|cf-turnstile/i.test(t))push(dom,"captcha-widget","CAPTCHA widget",90)}var nodes=document.querySelectorAll("script[src],iframe[src],link[href]");for(var i=0;i<nodes.length;i++){var src=nodes[i].src||nodes[i].href||"";if(/queue-it\\.net/i.test(src)){push(dom,"queue-it-asset","Queue-it page asset",100);break}}var signals=merge(dom),confidence=signals.reduce(function(m,s){return Math.max(m,s.confidence)},0),challenge=isChallengePage(t);return{live:challenge?false:confidence>=60,confidence:challenge?0:confidence,signals:signals,challenge:challenge}}function beaconUrl(state){var q=new URLSearchParams({sessionId:SID,live:state.live?"1":"0",confidence:String(state.confidence||0),token:TOK,pageUrl:String(location.href).slice(0,500),source:"bookmarklet"});try{q.set("signals",JSON.stringify((state.signals||[]).slice(0,8)))}catch(e){}return O+"/api/pokemon-center/report?"+q.toString()}function openBeacon(url){var opened=null;try{opened=window.open(url,"pcwBeacon")}catch(e){opened=null}if(opened){return true}try{var a=document.createElement("a");a.href=url;a.target="pcwBeacon";a.rel="opener";a.style.display="none";document.body.appendChild(a);a.click();a.remove();return true}catch(e){return false}}function setBadge(state,note){if(state&&state.live){b.textContent="PC Queue LIVE";b.style.background="#059669";return}if(state&&state.challenge){b.textContent="Drop guard UP";b.style.background="#b45309";return}if(note){b.textContent=note;b.style.background="#b45309";return}b.textContent=syncOk?"PokeWatch active · synced":"PokeWatch — click to sync";b.style.background=syncOk?"#111827":"#b45309"}function armConfirm(state){pendingState=state;if(confirmT)clearTimeout(confirmT);confirmT=setTimeout(function(){if(!syncOk){log("no sync confirm — allow pop-ups, re-copy bookmarklet from PokeWatch, run supabase/queue-watch.sql");setBadge(pendingState,"PokeWatch — sync failed, click badge")}confirmT=null},2800)}function beacon(state){var url=beaconUrl(state);var ok=openBeacon(url);if(ok){log("sync ping opened (waiting for CollecTools confirm)");setBadge(state,"PokeWatch — confirming…");armConfirm(state)}else{syncOk=false;log("pop-up blocked — click the orange badge to sync with CollecTools");setBadge(state,"PokeWatch — click to sync")}return ok}function report(state,force){var now=Date.now(),changed=lastRep===null||lastRep!==state.live;var challengeEdge=!!state.challenge&&!lastChallenge;if(!force&&!changed&&!challengeEdge&&now-lastAt<HB)return;beacon(state);lastAt=now;lastRep=state.live;lastChallenge=!!state.challenge;if(state.live&&!lastLive){try{if(typeof Notification!=="undefined"&&Notification.permission==="granted")new Notification("Pokemon Center queue is LIVE",{body:"Join the queue now.",tag:"pc-queue-live",requireInteraction:true})}catch(e){}}if(state.challenge&&!lastChallenge){try{if(typeof Notification!=="undefined"&&Notification.permission==="granted")new Notification("Pokemon Center drop guard is UP",{body:"Imperva verification detected — complete it now.",tag:"pc-imperva-challenge",requireInteraction:true})}catch(e){}}lastLive=state.live}window.addEventListener("message",function(ev){try{if(String(ev.origin)!==O)return;var d=ev.data;if(!d||d.type!=="pcw-sync")return;if(d.ok){syncOk=true;log("synced with CollecTools",d.pushSent?"queue push sent":d.challengePushSent?"challenge push sent":"no push",d.pushReason||"",d.challengePushReason||"");if(confirmT){clearTimeout(confirmT);confirmT=null}setBadge(pendingState||scan(),null)}else{syncOk=false;log("sync rejected",d.error||"");setBadge(pendingState,"PokeWatch — "+String(d.error||"auth failed").slice(0,42))}}catch(e){}});function onNet(url){if(!/queue-it\\.net/i.test(String(url||"")))return;push(sticky,"queue-it-net","Queue-it network request",100);report({live:true,confidence:100,signals:merge([{id:"queue-it-net",label:"Queue-it network request",confidence:100}]),challenge:false},true)}b.addEventListener("click",function(){log("manual sync");report(scan(),true)});var of=window.fetch;window.fetch=function(){try{onNet(arguments[0]&&arguments[0].url?arguments[0].url:arguments[0])}catch(e){}return of.apply(this,arguments)};var oo=XMLHttpRequest.prototype.open;XMLHttpRequest.prototype.open=function(m,u){try{onNet(u)}catch(e){}return oo.apply(this,arguments)};try{if(typeof PerformanceObserver!=="undefined"){var po=new PerformanceObserver(function(list){list.getEntries().forEach(function(e){onNet(e.name)})});po.observe({entryTypes:["resource"]})}}catch(e){}var mt=null;new MutationObserver(function(){if(mt)clearTimeout(mt);mt=setTimeout(function(){var s=scan();report(s,!!s.challenge&&!lastChallenge)},750)}).observe(document.documentElement,{childList:true,subtree:true,characterData:true});setInterval(function(){var s=scan();report(s,false)},SCAN);log("started session",SID);alert("CollecTools PokeWatch started.\\n\\n1) Click OK\\n2) Allow the CollecTools pop-up\\n3) Badge must say \\"active · synced\\" (not just active)\\n4) Keep this tab + PokeWatch page open");report(scan(),true)})();`
}

/** Same monitor without the javascript: prefix — paste into DevTools console on pokemoncenter.com */
export function buildQueueWatchConsoleSnippet(input: {
  origin: string
  sessionId: string
  token: string
}): string {
  const href = buildQueueWatchBookmarklet(input)
  return href.startsWith("javascript:") ? href.slice("javascript:".length) : href
}
